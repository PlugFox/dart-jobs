query FetchRecent(
    $after: timestamp!,
    $limit: Int! = 100,
    $exclude: [Int!]!
){
    job(
        where: {
            updated: {
                _gt: $after
            },
            deletion_mark: {
                _eq: false
            },
            id: {
                _nin: $exclude
            }
        },
        order_by: [
            {
                updated: asc
            }
        ],
        limit: $limit
    ) {
        id,
        creator_id,
        created,
        updated,
        deletion_mark,
        # Data:
        title,
        company,
        country,
        address,
        remote,
        relocation,
        employments,
        levels,
        is_english,
        is_russian
    }
}
