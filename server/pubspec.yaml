name: server
description: A server app using the shelf package and Docker.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.14.1 <3.0.0'

dependencies:
  meta: ^1.7.0
  args: ^2.0.0
  shelf: ^1.1.0
  shelf_router: ^1.0.0
  l: ^3.0.1
  rxdart: ^0.27.2

dev_dependencies:
  http: ^0.13.0
  lints: ^1.0.0
  test_process: ^2.0.0
  test: ^1.15.0
