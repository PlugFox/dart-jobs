name: dart_jobs_server
description: A server app using the shelf package and Docker.
version: 1.0.0
# homepage: https://www.example.com

publish_to: none

environment:
  sdk: '>=2.15.0-178.1.beta'

dependencies:
  meta: ^1.7.0
  args: ^2.0.0
  shelf: ^1.1.0
  shelf_router: ^1.0.0
  #shelf_proxy: ^1.0.1
  #shelf_static: ^1.1.0
  shelf_web_socket: ^1.0.1
  web_socket_channel: ^2.1.0
  l: ^3.0.2+1
  rxdart: ^0.27.2
  platform_info: ^3.1.0
  #grpc: ^3.0.2
  protobuf: ^2.0.0

  # Models
  dart_jobs_shared:
    path: ../shared

dev_dependencies:
  #build_runner: ^2.1.4
  #shelf_router_generator: ^1.0.1
  http: ^0.13.0
  lints: ^1.0.0
  test_process: ^2.0.0
  test: ^1.15.0
